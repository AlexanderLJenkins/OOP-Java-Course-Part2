package personnel;

/**
 * An enum type to represent an individual's education level.
 * @author Alex Jenkins
 */
public enum Education {
    // D (doctor), M (master), B (bachelor), GRAD (graduate).
    D, M, B, GRAD;
}
